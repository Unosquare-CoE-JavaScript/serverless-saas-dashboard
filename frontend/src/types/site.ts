export interface Site {
    site_id: string
    organization_id: string
    site_name: string
    description?: string
    site_url: string
    site_status: string
    site_keyword: string
    site_mailers: string[]
};